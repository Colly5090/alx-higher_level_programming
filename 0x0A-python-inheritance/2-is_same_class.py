#!/usr/bin/python3
""" A Module that check an instance of a class """


def is_same_class(obj, a_class):
    """ A Function that checks an object is an instance of a class """

    return isinstance(obj, a_class)
